# Images/

Dataset de 14 photos de paysages maritimes (côte, bateaux, mer, terre, ciel).

- Résolution : ~1000-2000px
- Format : JPEG
- Taille totale : ~33 Mo

Le chemin est configurable dans `config.py` (`IMAGES_DIR`).
Pour ajouter des images : copier dans ce dossier (JPG, PNG, WEBP supportés).
